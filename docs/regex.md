# ตารางอธิบาย Regex (เจ้าของกฎเป็นคนเติม)

ไฟล์นี้คือหลักฐานว่า "อธิบาย regex ได้" ซึ่งเป็นเกณฑ์ให้คะแนนข้อหนึ่ง
และเป็นสคริปต์ตั้งต้นสำหรับพูดในคลิป (คนละ ~1 นาที)

เขียนตามแม่แบบนี้ ทุกคนใช้หัวข้อเดียวกัน:

---

## 1. บัตรเครดิต — `credit_card` (คนที่ 2)

**Pattern**

```python
# TODO: วางของจริงที่นี่
```

**อธิบายทีละส่วน**

| ส่วน | ความหมาย |
|---|---|
| | |

**ทำไมถึงเลือกแบบนี้** —
**เคสที่จงใจไม่จับ** —
**ข้อจำกัดที่รู้ตัว** —

---

## 2. เบอร์โทรศัพท์ — `phone` (คนที่ 2)

(แม่แบบเดียวกัน)

---

## 3. อีเมล — `email` (คนที่ 3)

ประเด็นที่ต้องอธิบายให้ได้: ทำไมต้องใช้ `re.sub()` แบบส่ง callback (หรือเมธอด `mask()` ในโครงนี้)
แทนการส่ง string แทนที่ตรง ๆ — เพราะจำนวน `*` ขึ้นกับความยาว username ที่รู้ได้ตอน runtime เท่านั้น

---

## 4. วันเดือนปีเกิด — `dob` (คนที่ 4)

ประเด็นที่ต้องอธิบายให้ได้: `re` ไม่รองรับ lookbehind ความยาวไม่คงที่
จึงใช้ named group `(?P<target>...)` ครอบเฉพาะส่วนที่จะ mask แทน

---

## 5. ที่อยู่ — `address` (คนที่ 5)

ประเด็นที่ต้องอธิบายให้ได้: เลขที่บ้านกับเลขซอยหน้าตาเหมือนกัน แยกได้ด้วย "บริบท" เท่านั้น
และใช้ non-capturing group `(?:...)` กับส่วนที่เป็น optional

---

## เทคนิคที่โปรเจกต์นี้ใช้ (สรุปสำหรับพรีเซนต์)

| เทคนิค | ใช้ที่ไหน | ทำไม |
|---|---|---|
| `re.VERBOSE` | ทุกกฎ | เขียน pattern หลายบรรทัดพร้อมคอมเมนต์ได้ |
| named group `(?P<name>...)` | email, dob, address | อ้างถึงส่วนย่อยตอน mask และระบุช่วง `target` |
| non-capturing `(?:...)` | address, phone | จัดกลุ่มโดยไม่กินหมายเลข group |
| callback ตอนแทนที่ | ทุกกฎ (เมธอด `mask()`) | ผลลัพธ์ขึ้นกับความยาว/เนื้อหาของ match |
| lookaround | credit_card, phone | กันจับเลขที่ยาวเกินหรือติดกับตัวอักษรอื่น |
| post-validation | dob (วัน/เดือน), credit_card (Luhn) | เงื่อนไขที่เขียนเป็น regex แล้วอ่านไม่รู้เรื่อง |
